---
description: Implement a small explicit low-risk change with targeted validation
agent: quick-fix
---

Run the fast-path workflow for this request:

$ARGUMENTS

Classify the request before editing. Proceed only if it is explicit, localized,
low-risk, and has deterministic targeted validation. Otherwise stop without
editing and route me to the normal `/plan` → `/build` workflow.
